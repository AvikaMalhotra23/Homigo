package com.example.homigo.ui.theme

import androidx.compose.ui.graphics.Color

// ------------------------------
// Homigo semantic color system
// ------------------------------

// Primary (Sky)
val Primary = Color(0xFF38BDF8)
val PrimaryDark = Color(0xFF0EA5E9)

// Secondary (Mint)
val Secondary = Color(0xFF34D399)

// Accent (Aqua)
val Accent = Color(0xFF2DD4BF)

// Surfaces
val Background = Color(0xFFF8FAFC)
val Surface = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFF1F5F9)

// Text (Graphite)
val Graphite = Color(0xFF1F2937)
val SecondaryText = Color(0xFF64748B)
val Border = Color(0xFFE2E8F0)

// State colors
val Success = Color(0xFF10B981)
val Warning = Color(0xFFF59E0B)
val Error = Color(0xFFEF4444)

// ------------------------------
// Backwards-compatible aliases
// (Do not remove: existing code may reference them.)
// ------------------------------

// Former Material placeholder colors mapped to the new palette.
val Purple80 = Primary
val PurpleGrey80 = SecondaryText
val Pink80 = Accent

val Purple40 = PrimaryDark
val PurpleGrey40 = SecondaryText
val Pink40 = Accent
